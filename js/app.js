/**
 * Chorapp – Einstiegspunkt (Gerüst)
 *
 * Statische Single-Page-App ohne Build-Step (AR-001, NF-004).
 * Übungsdaten werden getrennt vom Code aus data/uebungen.json geladen (AR-002).
 *
 * Hinweis: In dieser Phase ist bewusst noch KEINE Logik implementiert.
 * Set-Generierung, Übungsdarstellung und eigene Übungen folgen in
 * späteren Sessions (FA-001 ff.).
 */

"use strict";

// Platzhalter für den späteren App-Start.
document.addEventListener("DOMContentLoaded", function () {
  // Folgt in einer späteren Session: Daten laden, Set generieren, rendern.
});
